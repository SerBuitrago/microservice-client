spring:
  application:
    name: pragma-gateway
  cloud:
    config:
      enabled: true
      uri: http://localhost:8084
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
