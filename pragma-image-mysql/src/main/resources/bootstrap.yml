spring:
  application:
    name: pragma-image-mysql
  cloud:
    config:
      enabled: true
      uri: http://localhost:8084 
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:      
      show-details: always
